import * as types from './types'

const initState = {

}

const reducer = (state = initState, {type, payload}) => {
    switch(type) {
        default:
            return state
    }
}

export default reducer